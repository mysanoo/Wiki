server:
  port: 8080
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/WikiHow
    password: KXumora1220
    username: postgres
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
        hibernate:
          globally_quoted_identifiers: true

